[build]
  base    = "frontend"
  command = "npm ci --no-audit --no-fund && npm run build"
  publish = "dist/frontend/browser"

[build.environment]
  NODE_VERSION = "20.19.0"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
